#import "../vision.cpp"

namespace ot { // ot stands for Object Tracking I think?
	namespace cube {
		void findCubes(Mat frame) {
			// do stuff
		}
	};

	void tick(Mat frame) {
		cube::findCubes(frame);
	}
}
